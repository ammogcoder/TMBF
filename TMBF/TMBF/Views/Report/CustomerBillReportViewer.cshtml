@model TMBF.Models.SearchParameterModel
@{
    ViewBag.Title = "Customer bill";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script type="text/javascript">
</script>
<hgroup class="title">
    <h1>@ViewBag.Title</h1>
</hgroup>

<section id="ReportViewer">
    @using (Html.BeginForm(new { ReturnUrl = ViewBag.ReturnUrl }))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)


        <fieldset>
            <ol>
                <li>
                    @Html.LabelFor(m => m.Month)
                    @Html.TextBoxFor(m => m.Month, new { @Value = DateTime.Today.Month })
                    @Html.ValidationMessageFor(m => m.Month)
                </li>
            </ol>
            <ol>
                <li>
                    @Html.LabelFor(m => m.Year)
                    @Html.TextBoxFor(m => m.Year, new { @Value = DateTime.Today.Year })
                    @Html.ValidationMessageFor(m => m.Year)
                </li>
            </ol>
            <input type="submit" class id="btnSubmit" value="View Report" />
        </fieldset>
    }
</section>
@if (Model != null)
{
    <section class="social" id="ReportDisplayPanel">
        <img src="@Url.Action("GenerateAndDisplayReport", "Report", new { month = Model.Month, year = Model.Year, format = "pdf" })" />
    </section>
}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

